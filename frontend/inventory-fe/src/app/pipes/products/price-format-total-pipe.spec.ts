import { PriceFormatTotalPipe } from './price-format-total-pipe';

describe('PriceFormatTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceFormatTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
