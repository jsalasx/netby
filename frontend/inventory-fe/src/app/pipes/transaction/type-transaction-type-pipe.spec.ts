import { TypeTransactionTypePipe } from './type-transaction-type-pipe';

describe('TypeTransactionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeTransactionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
